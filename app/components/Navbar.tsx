import React from "react";

const Navbar = () => {
  return <DesktopNavbar />;
};

const DesktopNavbar = () => {
  return (
    <nav className="bg-gray-800">

    </nav>
  );
};

export default Navbar;
